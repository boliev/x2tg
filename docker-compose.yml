version: '3'
services:
  db:
    image: postgres:16.1
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: x2tg
      POSTGRES_DB: x2tg
      POSTGRES_PASSWORD: 123456